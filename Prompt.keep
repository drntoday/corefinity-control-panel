Task: Fully implement the "Deployments" sub-tab logic in Environments.jsx using React state.
​1. Form State Logic:
​Create state variables for: pipeline, provider, repository, and branch.
​Strict Dependency Chain: >    - Provider dropdown must have disabled={!pipeline}.
​Repository dropdown must have disabled={!provider}.
​Branch dropdown must have disabled={!repository}.
​2. The "No Account" Warning:
​If provider === 'GitHub', render a warning box: "⚠️ No account connected. [Link GitHub Account]". The link must use the brand-orange color.
​3. Deployment Task List (Real-time Feel):
​Below the form, create a table for "Recent Deployment Tasks."
​Top Row: Set status to "In Progress" with a pulsing green dot.
​Animate: Add a progress bar that uses a useEffect to move from 0% to 100%.
​Data Columns: Commit Hash (e.g., 8f2d3a1), Message ("Update styles"), Author ("Catherine J."), and Status.
​4. Sub-Tab Switching Fix:
​Ensure that when the user clicks the "Deployments" tab in the sub-navigation, the activeSubTab state updates to show this specific component and hides the "General" info.
​5. Visual Polish:
​Use the Corefinity Orange (#FF6B00) for all links and the "Deploy" button.
​Use a monospace font for the Commit Hash to give it a technical, high-end feel.
