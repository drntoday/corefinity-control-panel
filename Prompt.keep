Task: Finish the Environments Tab to strictly resonate with Corefinity instructions.
​1. Sequential Deployment Form (Image 11):
​Create a React state deploymentStep.
​Logic: Pipeline dropdown (Step 0) -> Provider (Step 1) -> Repository (Step 2) -> Branch (Step 3).
​Set disabled={deploymentStep < X} for each dropdown.
​If Provider === 'GitHub', show a Red Alert: "No account connected. [Link GitHub Account]".
​2. High-Density Technical Data (Pods/Nodes):
​Populate the Pods tab with a table: Pod Name | Status | CPU % | Memory | Actions.
​Use a setInterval to make the CPU and Memory numbers change every 2 seconds to simulate a live server.
​Status should be a "Pulsing Green Dot" for 'Running'.
​3. Interactive Diagnostics (Terminal View):
​In the Diagnostics tab, create a black box component. When a "Run Diagnostics" button is clicked, have it "type out" lines: Checking SSL... OK, Testing Database... OK, Latency: 12ms.
​4. Functional Firewall (Image 13):
​Add a "Use My Current IP" button next to the IP input field.
​Ensure the list of IPs has a "Delete" icon and a "Rule Type" label (e.g., "Allow").
​5. Global Orange Link Audit:
​Wrap all instance of Company Names, Website Names, and Environment IDs in the OrangeLink component.
​Ensure the top breadcrumb matches the hierarchy: All Clients > Example Company > Website Name > Environment.
